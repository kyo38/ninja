# config.toml
# マネージャーが起動時に動的に読み込むワーカーノードの一覧

worker_addresses = [
    "127.0.0.1:9000",
    "127.0.0.1:9001"
]

# ハートビート（死活監視）の設定
[heartbeat]
interval_secs = 3      # 3秒ごとに各ワーカーの生存を確認
timeout_secs = 1       # 1秒以内に応答がなければダウンとみなす