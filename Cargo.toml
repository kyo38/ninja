[package]
name = "ninja"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8"
anyhow = "1.0" # 既存のパケット・パス処理に必要なため、ここに追加します
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"